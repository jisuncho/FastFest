<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="Member">

	<!-- 쿼리를 xml로 작성하고 id:식별값  
		  parameterType:우리가 전달할값 
	     resultType : 받아오는값  -->
	     
	<!-- if test="key=='1'"  - 이렇게 쓰면 안된다 인식안된다 -->
	
	
	<select id="Member_isduplicate" parameterType="MemberDto" resultType="Integer">
		select count(*) from pro1_member
		where user_id=#{user_id}
	</select>
	
	<insert id="Member_insert" parameterType="MemberDto" >
	INSERT INTO pro1_member
	(
		user_id,
		user_password,
		user_name,
		user_mail,
		user_phone
	)
	VALUES
	(
		#{user_id},
		#{user_pw},
		#{user_name},
		#{user_mail},
		#{user_phone}
	)
	</insert>
	
	<select id="Member_getInfo" parameterType="MemberDto" resultType="MemberDto">
		select 	 
			user_id
			,user_password
			,user_name
			,user_mail
			,user_phone
		from pro1_member
		where user_id=#{user_id} 
	</select>
	
	<select id="Member_findid" parameterType="MemberDto" resultType="MemberDto">
		select 	
			user_id 
			,user_password
			,user_name
			,user_mail
			,user_phone 
		from pro1_member
		where user_mail=#{user_mail} and user_phone=#{user_phone} 
	</select>
	
	<select id="Member_findpassword" parameterType="MemberDto" resultType="MemberDto">
		select 	
		    user_id
			,user_password
			,user_name
			,user_mail
			,user_phone 
		from pro1_member
		where user_mail=#{user_mail} and user_id=#{user_id} 
	</select>
			
	<update id="Member_updateInfo" parameterType="MemberDto" >
		update pro1_member set user_password = #{user_pw}
						,user_mail=#{user_mail}
						,user_phone=#{user_phone}
		where user_id=#{user_id}
	</update>

	
</mapper>




















